---
name: design-to-tasks
description: docs/frontend/design/*.md の設計書1件からdocs/frontend/tasks/配下に実装計画（Codex向けタスクファイル群）を生成する。「設計書からタスクに落とす」「実装計画を作って」「タスク分解して」「design/xxx.mdをtasksにして」のような依頼、あるいはフロントエンドのdesignドキュメントを指してCodexへの引き継ぎ計画を求められたときに使う。ユーザーが対象ファイルを明示していない場合は必ず先にどの設計書か確認する。
---

# design-to-tasks

`docs/frontend/design/*.md` の設計書1件を、Codexが実装できる粒度のタスク計画に分解し、`docs/frontend/tasks/<フォルダ名>/` に書き出すスキル。設計・計画はClaude（このスキルの実行者）が担い、実際のコード実装とテストはCodexに委譲する運用を前提にしている。

## 全体の流れ

1. **対象ファイルの確定**：ユーザーが設計書パスを指定していなければ、`docs/frontend/design/`の一覧を見せて1件を選んでもらう。複数件を一度に依頼された場合も、1件ずつ順番に処理する（設計書1件 = tasksサブフォルダ1つが原則）。
2. **設計書を読む**：対象の設計書全文を読む。他の設計書やUI HTML（`docs/frontend/ui/*.html`）を横断的に読み込む必要はない。設計書中に「参照: `docs/frontend/ui/xxx.html`」のような明示的な対応モックの記載があれば、その1ファイルだけは併読してよい。
3. **既存のtasksフォルダ構成を確認する**：`docs/frontend/tasks/`配下の既存フォルダ（特に `01_common/`）に目を通し、フォーマット・粒度・チェックボックスの書き方をこのスキルの出力に合わせる（下記テンプレートと矛盾する独自ルールが既にあれば、それに寄せる）。
4. **機能単位に分解する**：下記「タスク分解の考え方」に従い、設計書を独立して実装・レビューできる単位に割る。
5. **フォルダとファイルを書き出す**：`docs/frontend/tasks/<フォルダ名>/`に `00_overview.md` + 機能ごとのタスクファイル + `open_questions.md` を作成する。
6. **要約を報告する**：作成したファイル一覧と、判断に迷った点（フォルダ名・粒度など）を短くユーザーに報告する。

## タスク分解の考え方

分解の基準は「Codexが1回のセッションで着手から完了（lint/test/build green）まで終えられる粒度」かどうか。目安：

- 設計書が単一画面（例: `01_home.md`）を扱っている場合、基本は画面本体で1ファイル + 必要なら画面固有の複雑なサブコンポーネント群で1ファイル。無理に分割しない。
- 設計書が複数画面・共通基盤を横断する場合（例: `00_common.md`）、トークン/CSS基盤・レイアウト骨格・共通コンポーネント群・詳細レイアウトのように、依存関係の低い塊ごとに分割する（`01_common/`の既存4ファイル構成が参考例）。
- どの粒度でも、各タスクファイルは「前提ファイル（参照許可の範囲）」を独立して定義できることを分割の判断基準にする。参照範囲が重なりすぎる/循環する場合は分割をやめて1ファイルにまとめる。
- 迷ったら過剰分割より過少分割を選ぶ。ファイルを増やすほどCodexとのやり取り回数が増え、依存関係の管理コストが上がる。

## フォルダ・ファイル命名

- フォルダ名: 設計書ファイル名から拡張子を除いたものをそのまま使う（例: `02_general_media.md` → `docs/frontend/tasks/02_general_media/`）。ただし `docs/frontend/tasks/`に既にその設計書に対応するフォルダがある場合（overview.md内の「対応: 設計書 §…」記述などで確認）は新規作成せず追記・更新する。命名に迷う場合（設計書番号とtasksの既存番号がずれているなど）はユーザーに確認する。
- ファイル名: `00_overview.md` を先頭に、`01_`, `02_`... と機能ごとのタスクファイルを続け、最後に `NN_open_questions.md` を置く。

## 出力テンプレート

### `00_overview.md`

```markdown
# <フォルダ名> 実装タスク概要

`docs/frontend/design/<設計書ファイル名>`（以下「設計書」）を実装に落とし込むための計画。対象は<スコープを1文で>。

## タスクファイル構成

| ファイル | 内容 | 対応する設計書セクション |
|---|---|---|
| [01_xxx.md](01_xxx.md) | <内容> | §.. |
| ... | ... | ... |
| [NN_open_questions.md](NN_open_questions.md) | 設計書中の【要確認】項目の追跡と決定事項の記録 | §.. |

## Claude ⇄ Codex 運用ルール

このタスク群は Claude が計画・レビューを行い、実装は Codex に委譲する前提で書式を統一している。

- 各タスクファイルの「タスク一覧」「テストリスト」のチェックボックス `- [ ]` は、実装・テストが完了するたびに **Codexが** `- [x]` に変更する（Claude側では変更しない）。
- タスクを完了する際、対応するタスク見出しの直下にある `> Codexメモ:` 行に、実装上の判断・設計書との差分・未解決事項を1〜3行で追記する（無ければ `> Codexメモ: (なし)` のままでよい）。
- 【要確認】に該当する意思決定が必要になった場合は、実装を進めず [NN_open_questions.md](NN_open_questions.md) に追記し、Claudeのレビューを待つ。ブロッキングでない場合は妥当な仮決定を行い、その旨をメモに残して先に進んでよい。
- 依存関係: `01_xxx` → `02_yyy` → … の順に着手する（後続タスクは前段に依存するため）。
- **実装範囲の限定**: 着手するタスクの「タスク一覧」に書かれた内容の実装のみを行う。参照してよいファイルは各タスクファイルの「前提ファイル」節に列挙されたものと、そこから直接importされるファイルに限定する。それ以外のファイルを探すための横断的なコードベース探索（grep/find等での関連ファイル探索、他タスクの実装状況の確認等）は行わない。前提ファイルの範囲で判断がつかない場合は、推測で探索を広げず [NN_open_questions.md](NN_open_questions.md) に記載するか、タスク側で仮決定してCodexメモに記載する。

## 完了の定義（DoD）

各タスクは以下をすべて満たして完了とする。

- [ ] `yarn lint` が通る
- [ ] `yarn test` が通る（対象タスクのユニットテストを含む）
- [ ] `yarn build`（`tsc -b && vite build`）が型エラーなく通る
- [ ] 設計書の該当セクションで定義されたprops/クラス名/挙動と実装が一致している（差異があればCodexメモに記載）
- [ ] `docs/frontend/ui/*.html` + `_shared.css` の見た目・DOM構造とTailwind実装がセマンティックに一致している（対応モックがある場合）
- [ ] 対応モックがあるタスクは `yarn test:e2e` の実装⇔モック レイアウト一致テストが通る
```

`yarn lint` / `yarn test` / `yarn build` はこのリポジトリの `frontend/package.json` を確認し、実際に定義されているスクリプト名に置き換える。無ければ同等のコマンドを調べて使う。

### 機能ごとのタスクファイル（`01_xxx.md`など）

```markdown
# <連番>. <タスク名>

対応: 設計書 §<セクション番号>

依存: [<前段のファイル名>](<前段のファイル名>) 完了後に着手。（最初のタスクなら省略可）

## 前提ファイル

- 参照: `<設計書がこのタスクのために参照してよいと明言しているファイルパス。UIモック、既存コンポーネント、型定義など>`
- 出力: `<このタスクで新規作成/変更するファイルパスを列挙>`
- 参照範囲はこの節に列挙したファイルとそこから直接importされるファイルに限る。それ以外のファイルを探すための横断的な探索は行わない。

## タスク一覧

- [ ] <実装項目1（設計書のprops名・クラス名・API仕様など具体的に書く）>
- [ ] <実装項目2>
- ...

## テストリスト

- [ ] <テストファイル名>: <検証内容1>
- [ ] <テストファイル名>: <検証内容2>
- ...
- [ ] `tests/e2e/<画面名>.spec.ts`: `yarn dev`起動下で対象画面/コンポーネントを実描画し、対応モック `docs/frontend/ui/<対応html>` とレイアウトが一致することを確認する（対応モックが無いタスクは省略可。省略する場合はその旨をタスク一覧末尾に一言添える）

> Codexメモ: (なし)
```

- 「タスク一覧」「テストリスト」の各項目は、設計書の記述（props型・クラス名・API・インタラクション仕様など）を具体的に反映させる。抽象的な「〜を実装する」だけで終わらせず、設計書のどの情報を使うべきかがCodexだけで判断できるレベルまで書き下す。
- 「前提ファイル」の「参照」には、設計書自身・対応するUIモック（`docs/frontend/ui/*.html`, `_shared.css`）・依存する前段タスクの出力ファイルなど、実際に必要なものだけを列挙する。過不足なく絞ることが「実装に関係ないファイルを参照させない」制約の核心。
- **モックとのレイアウト一致確認テストは必須**。対応するUIモック（`docs/frontend/ui/*.html`）が存在するタスクでは、`yarn test:e2e`（Playwright, `frontend/playwright.config.ts` で `webServer: yarn dev` が既に設定済み）で実描画結果とモックのレイアウトを突き合わせるe2eテストを必ずテストリストに含める。比較観点は最低限「主要な構造要素（grid/section/カード等）の有無と並び順」「モックのクラス名相当の要素が対応する位置に存在すること」。ピクセル単位のスクリーンショット比較まで求める必要はなく、DOM構造・主要領域のレイアウト一致で十分。具体的な比較手段（`getByRole`/`locator`でのDOM構造アサーション、もしくはスクリーンショット比較のどちらを使うか）は各タスクの複雑さに応じてタスク一覧側で指定する。

### `NN_open_questions.md`

```markdown
# <フォルダ名> 未決事項

設計書中の【要確認】項目、およびタスク実装中にCodexが仮決定した事項を記録する。

## 未決事項

- [ ] <設計書の【要確認】記述をそのまま引用 or 要約> (出典: §<セクション>)

## Codexによる仮決定ログ

- (Codexが仮決定した場合、ここに追記される)
```

## 注意点

- 設計書に明示のない実装詳細を勝手に補完しない。判断が必要な箇所は `open_questions.md` に回す。
- **`frontend/src/index.css` の拡張について**: コンポーネント単位で機能分割した結果、`docs/frontend/ui/_shared.css`に定義が無いクラス/タグ（コンポーネント内部でのみ使う補助クラスなど）が必要になることがある。この場合、`frontend/src/index.css`への追加自体は許可するが、`_shared.css`が定義しているレイアウト（`.app-shell`/`.card-grid`/`.detail-layout`等の既存クラスのgrid構成・余白・ブレークポイント等）を上書き・変更してはならない。該当タスクの「前提ファイル」の「参照」に `docs/frontend/ui/_shared.css` と `frontend/src/index.css` を含め、タスク一覧に「`_shared.css`に対応クラスが無い場合のみ`frontend/src/index.css`に追加してよい。既存クラスの値は変更しない」旨を明記する。
- 既存の `frontend/src/` 構成（コンポーネント配置、`cn`ユーティリティ、テスト方針など）を軽く確認し、出力ファイルパスの提案が既存の慣習と矛盾しないようにする。ただし詳細な実装コードは書かない（実装はCodexの仕事）。
- 生成後、ユーザーに分割方針とフォルダ名を短く報告し、次の設計書に進むかを確認する。
