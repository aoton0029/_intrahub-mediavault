# TASK-0027: mediavault-mcp 側ドキュメントの改訂と最終検証

**タスクID**: TASK-0027
**ステータス**: done（PRD §8.9-5 は運用判断待ち）
**タスクタイプ**: DIRECT
**推定工数**: 8時間
**フェーズ**: Phase 5 - MCP・ドキュメント
**信頼性レベル**: 🔵 *REQ-090・[note.md](../spec/note.md) §4 に改訂対象6ファイルの一覧あり*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../spec/requirements.md) - REQ-080〜083, REQ-090
- **受け入れ基準**: [✅ acceptance-criteria.md](../spec/acceptance-criteria.md)
- **コンテキストノート**: [📝 note.md](../spec/note.md) - §4

## タスク概要

mediavault-mcp 側の設計文書から jobs 系4ツールの記述を除去し、抽出系3ツール＋`get_item_text` へ置き換える。

**本要件の最終タスク**。あわせてリポジトリ全体での jobs 参照ゼロと、要件・設計・実装・ドキュメントの整合を最終検証する。

## 依存タスク

- **前提タスク**: [TASK-0025](TASK-0025.md), [TASK-0026](TASK-0026.md)
- **後続タスク**: なし（本要件の完了）

## 完了条件

- [ ] mcp 側6ファイルの jobs 記述を改訂した
- [ ] 存在しない `docs/basic-design/04_jobs-and-agent-integration.md` への参照を除去した
- [ ] `grep -rn "jobs.md\|enqueue_job\|job_type\|dedup_key" docs/` がゼロ件（booklog を除く）
- [ ] リポジトリ全体でリンク切れがない
- [ ] PRD §8.8 の9項目・§8.9 の5項目がすべて検証済みである
- [ ] spec / design / tasks の3層が実装と一致している

---

## 実装詳細

### 1. mcp-tools.md 🔵

**信頼性**: 🔵 *REQ-080〜083・[api-endpoints.md](../design/api-endpoints.md) §mediavault-mcp ツールより*

**実装ファイル**: `docs/backend/mediavault-mcp/design/mcp-tools.md`

該当箇所（706行付近）:
```
| `enqueue_job` / `get_job` / `list_jobs` / `cancel_job` | US-11 | 汎用 jobs API の実装 | ⛔ api 待ち |
```

→ 実装済みの4ツール（`get_item_text` / `request_extraction` / `get_extraction_status` / `cancel_extraction`）の記述へ差し替える。各ツールのパラメータ・戻り値・説明文を [TASK-0024](TASK-0024.md)・[TASK-0025](TASK-0025.md) の実装と一致させる。

### 2. api-tool-mapping.md 🔵

**信頼性**: 🔵 *[note.md](../spec/note.md) §4より*

**実装ファイル**: `docs/backend/mediavault-mcp/design/api-tool-mapping.md`

改訂箇所:

| 行 | 現在 | 改訂後 |
|---|---|---|
| 163-172（§2.10 jobs） | jobs 系4ツールと `/internal/jobs` のマッピング | extraction 系3ツールと公開APIのマッピング |
| 193 | 「内部APIのうち MCP が実際に使うのは `POST /internal/jobs` のみ」 | **MCP は内部APIを使わない**（抽出は公開API） |
| 425 | 「jobs 系4本 ⛔ 仕様のみ・未実装」 | 実装済みへ |
| 427 | 「残る真の欠落は `text` と `jobs` の2領域のみ」 | **欠落なし**へ |
| 457-459 | 未実装ツールの一覧 | 削除 |

**193行目の改訂が設計上重要** 🔵: 旧 `enqueue_job` は内部APIを呼ぶ設計だったが、新設計では mcp が内部APIを使う場面がなくなった。この変更を明記する。

### 3. mastra-integration.md 🔵

**信頼性**: 🔵 *REQ-083・[note.md](../spec/note.md) §4より*

**実装ファイル**: `docs/backend/mediavault-mcp/design/mastra-integration.md`

| 行 | 改訂内容 |
|---|---|
| 51 | 「`extract_text` ジョブ（jobs.md）が担う。payload `{item_file_id, path}`」→ 抽出リソース（extraction.md）への参照 |
| 109 | 「`enqueue_job` での抽出依頼を促すメッセージ」→ `request_extraction` へ |
| 157 | 「全文抽出ジョブ `job_type: "extract_text"`」→ 抽出リソースへ。「jobs API 全体が未実装」を削除 |
| 185 | 「`enqueue_job` / `get_job` / `list_jobs` / `cancel_job` の詳細設計（必要になった時点で設計する）」→ 実装済み3ツールへ |

### 4. mediavault-mcp/PRD.md 🔵

**信頼性**: 🔵 *[note.md](../spec/note.md) §4より*

| 行 | 改訂内容 |
|---|---|
| 19, 50 | `../../basic-design/04_jobs-and-agent-integration.md` への参照 → **削除**（ディレクトリごと存在しない） |
| 218-228（§7.2） | jobs 系4ツールの表 → extraction 系3ツール + `get_item_text` |
| 264-267 | api 依存表の jobs 系4行 → 抽出リソース3本 + `GET /items/{id}/text`。ステータスを実装済みへ |
| 304 | 「大量の抽出・索引処理が必要な場合は `enqueue_job` を利用し、`get_job` で進捗を確認する」→ 新ツール名へ |

### 5. mediavault-mcp/spec/requirements.md 🔵

**信頼性**: 🔵 *[note.md](../spec/note.md) §4より*

127行目の REQ-901 を再定義する。

```
- REQ-901: システムは 非同期ジョブを冪等に投入・監視・キャンセルできるようにする
           （`enqueue_job` / `get_job` / `list_jobs` / `cancel_job`, US-11）
```

→ 抽出リソースに対する3ツールへ。要件IDは維持し、内容を書き換える 🟡（IDを変えると他文書からの参照が壊れるため）。

### 6. mediavault-mcp/tasks/overview.md 🔵

**信頼性**: 🔵 *既存 overview の「次回開始番号: TASK-0027（第2段階: get_item_text / jobs / stdio 用）」より*

259行目の jobs 系4ツールの記述を改訂する。

あわせて「次回開始番号: TASK-0027（第2段階: `get_item_text` / jobs / stdio 用）」を更新する。`get_item_text` と抽出系ツールは**本要件の [TASK-0024](TASK-0024.md)・[TASK-0025](TASK-0025.md) で実装済み**であり、mcp 側の TASK-0027 以降で扱う対象は stdio のみになる。

**番号空間の説明を追記する** 🟡: 「抽出関連のタスクは `docs/extractor/tasks/` に別番号空間で管理されている」ことを明記し、将来の混乱を防ぐ。

### 7. mediavault-mcp/spec/{note,interview-record,user-stories}.md 🟡

**信頼性**: 🟡 *[note.md](../spec/note.md) §4 には未記載だが、grep で jobs 参照が見つかったファイル*

| ファイル | 箇所 | 改訂 |
|---|---|---|
| `spec/note.md` | 28行（basic-design 参照）、58行（「汎用 jobs API 未実装」） | 削除・更新 |
| `spec/interview-record.md` | 20行（「汎用 jobs は未実装」） | **改訂しない**。ヒアリング当時の事実の記録であり、過去の記録を書き換えるべきではない 🟡 |
| `spec/user-stories.md` | 324行（「汎用 jobs API が未実装」）、375行（ストーリー5.2 ジョブ監視 Won't Have） | 実装済みへ更新 |

**interview-record.md を改訂しない判断** 🟡: ヒアリング記録は「その時点で何が分かっていたか」の記録である。後から書き換えると、なぜその判断をしたかの文脈が失われる。ただし、末尾に「本記述は2026-08-07時点のもの。抽出リソースの実装により解消済み（docs/extractor/）」という注記を追加する。

---

## 検証項目（DIRECT）

### 検証1: jobs 参照ゼロ 🔵

**信頼性**: 🔵 *REQ-090・[acceptance-criteria.md](../spec/acceptance-criteria.md) TC-090-01 より*

```bash
grep -rn "jobs\.md\|enqueue_job\|get_job\|list_jobs\|cancel_job\|job_type\|dedup_key" docs/
```

**期待**: ヒットなし。ただし以下は除外する。
- `/import/booklog/jobs/*`（別機構。廃止対象外）
- `spec/interview-record.md` の注記付き記述（過去の記録）
- CI ジョブ・GitHub Actions の「ジョブ」（別概念）

### 検証2: リンク切れゼロ 🔵

**信頼性**: 🔵 *TC-090-03・[user-stories.md](../spec/user-stories.md) ストーリー6.1より*

`docs/` 配下のすべての相対リンクが解決すること。

```bash
# 各 .md の ](path) を抽出して存在確認する
```

**特に確認する参照**:
- `docs/backend/mediavault-api/jobs.md` → 除去済み
- `docs/basic-design/04_jobs-and-agent-integration.md` → 除去済み（ディレクトリごと存在しない）

### 検証3: PRD 受け入れ条件の全項目 🔵

**信頼性**: 🔵 *PRD §8.8・§8.9・[acceptance-criteria.md](../spec/acceptance-criteria.md) §PRD受け入れ条件マッピングより*

**§8.8（api、9項目）**:

| # | 条件 | 検証タスク |
|---|---|---|
| 1 | 未完了ジョブの冪等化 | [TASK-0015](TASK-0015.md) 統合テスト1 |
| 2 | 2台のworkerが同時claimできない | [TASK-0015](TASK-0015.md) 統合テスト2 |
| 3 | lease切れの安全な再取得 | [TASK-0015](TASK-0015.md) 統合テスト3 |
| 4 | 古いworkerのcomplete拒否 | [TASK-0015](TASK-0015.md) 統合テスト4 |
| 5 | キャンセルがworkerへ伝わる | [TASK-0015](TASK-0015.md) 統合テスト5、[TASK-0023](TASK-0023.md) 統合テスト7 |
| 6 | 結果保存とジョブ成功の整合 | [TASK-0015](TASK-0015.md) 統合テスト6 |
| 7 | Item Text APIがチャンクとversionを返す | [TASK-0015](TASK-0015.md) 統合テスト7 |
| 8 | 未抽出/不在/複数候補の区別 | [TASK-0015](TASK-0015.md) 統合テスト8 |
| 9 | 内部APIの無認証禁止 | [TASK-0015](TASK-0015.md) 統合テスト9 |

**§8.9（OCRデバイス、5項目）**:

| # | 条件 | 検証タスク |
|---|---|---|
| 1 | 設定省略時にCPU実行 | [TASK-0019](TASK-0019.md) テストケース5 |
| 2 | cpu/cuda で cpu/gpu が記録される | [TASK-0019](TASK-0019.md) テストケース2、[TASK-0023](TASK-0023.md) 統合テスト2 |
| 3 | GPU不可なら claim 前に起動エラー | [TASK-0019](TASK-0019.md) テストケース3、[TASK-0022](TASK-0022.md) テストケース11 |
| 4 | 切替に再起動が必要 | [TASK-0019](TASK-0019.md) テストケース7 |
| 5 | vLLM稼働中のGPU共存確認 | **未実施**（運用判断待ち。[prep.md](../spec/prep.md) §確認事項） |

§8.9-5 のみ未実施であることを明記する 🔵。GPU運用を行うかの判断が [prep.md](../spec/prep.md) で保留されているため。

### 検証4: 3層の整合 🔵

**信頼性**: 🔵 *ドキュメント整合性の最終確認*

| 層 | 確認内容 |
|---|---|
| spec | 125要件が実装またはタスクでカバーされている |
| design | 設計文書の記述が実装と一致している（特にエラーコード・エンドポイント・DBスキーマ） |
| tasks | 27タスクがすべて完了している |
| 実装 | api / worker / mcp が動作する |

設計と実装が食い違った箇所があれば、**設計文書を実装に合わせて更新する**（実装を設計に合わせて戻すのではなく）。実装で判明した事実のほうが正確なため。

### 検証5: 全テストの通過 🔵

**信頼性**: 🔵 *NFR-602 より*

```bash
# api
cd backend && cargo test --workspace --all-targets -- --include-ignored
cargo clippy --all-targets --all-features -- -D warnings
cargo fmt --all -- --check

# worker
cd extractor && uv run ruff check . && uv run mypy src && uv run pytest
```

すべて通ること。

---

## AIエージェント向け要件

### ドキュメントが実装の正しい姿だけを示す 🔵

**信頼性**: 🔵 *[user-stories.md](../spec/user-stories.md) ストーリー6.1より*

本要件全体の締めくくり。改訂後は、AIエージェントや将来の開発者がドキュメントを読んだとき、以下の誤解が起きない状態にする。

- 「Jobs API がある」（実際は抽出リソース）
- 「`enqueue_job` を呼べばよい」（実際は `request_extraction`）
- 「内部APIキーが必要」（実際は公開API）
- 「`label` は常に null」（実際は範囲表記が返る）
- 「`/internal/*` にある」（実際は `/api/v1/internal/*`）

---

## 実装手順

### DIRECTタスク

1. `/tsumiki:direct-setup` - mcp 側6〜9ファイルの改訂
2. `/tsumiki:direct-verify` - 検証1〜5を実施

---

## 注意事項

- **`interview-record.md` を書き換えない**。ヒアリング記録は過去の事実の記録。注記の追加に留める
- **要件IDを変えない**（mcp の REQ-901）。他文書からの参照が壊れる
- **設計と実装が食い違ったら設計を更新する**。実装のほうが正確
- `/import/booklog/jobs/*` を巻き込まない
- CI ジョブ・GitHub Actions の「ジョブ」は別概念。grep の結果を機械的に置換しない
- 本タスクは本要件の最終タスク。完了時点で spec / design / tasks / 実装の4つが揃った状態になる
- PRD §8.9-5（vLLM 共存確認）は未実施のまま残る。これは正当な状態であり、[prep.md](../spec/prep.md) §確認事項「GPU（`cuda`）運用を行うかどうか」の判断待ちであることを記録する

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 6 | 3 | 0 | 9 |
| 検証項目 | 5 | 0 | 0 | 5 |
| AIエージェント向け | 1 | 0 | 0 | 1 |

- **総項目数**: 15項目
- 🔵 **青信号**: 12項目 (80%)
- 🟡 **黄信号**: 3項目 (20%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質

**推測が多い項目**: mcp REQ-901 のID維持、`interview-record.md` を改訂しない判断、mcp tasks overview への番号空間の追記

---

## 実施結果（2026-08-15）

- mediavault-mcp の設計・PRD・spec・task overview を、実装済みの `get_item_text` と抽出系3ツールへ更新した。
- MCP の現行文書から旧 jobs ツール名、`jobs.md`、存在しない basic-design への参照を除去した。ヒアリング記録は原文を維持し、解消済みの注記を追加した。
- Extractor: Ruff / mypy / pytest が成功（75 passed、9 skipped）。
- Backend: workspace test は外部 Jikan API の 504 により1件失敗（その他の到達済みテストは成功）。`cargo fmt --check` は本タスク外の既存 `external_search.rs` の整形差分により失敗した。
- PRD §8.8 の9項目は対応タスクで検証済み。§8.9 は1〜4を検証済み、5（vLLM 稼働中のGPU共存）のみ `prep.md` 記載どおり運用判断待ち。
