# MediaVault

メディア管理システム。バックエンド（Rust）・フロントエンド（React/TypeScript）・DB（PostgreSQL）で構成。

## 実装前に読むスキル

Before starting any implementation task that is ambiguous, unfamiliar, likely to take more than a few steps, or likely to affect architecture, UX, security, data models, cost, or reviewer buy-in, read and use the `unknowns-field-guide` skill.
