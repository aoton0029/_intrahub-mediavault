# IntraHub の services/mediavault/compose.yaml を単体起動用に変換した構成。
#
# 起動例:
#   cp .env.example .env
#   docker compose up -d --build

services:
  mediavault-postgres:
    image: postgres:16.9-alpine
    restart: unless-stopped
    environment:
      TZ: ${TZ:-UTC}
      POSTGRES_USER: mediavault
      POSTGRES_DB: mediavault
      POSTGRES_PASSWORD: ${MEDIAVAULT_DB_PASSWORD:?MEDIAVAULT_DB_PASSWORD is required}
    volumes:
      - ${MEDIAVAULT_DB_SOURCE:-mediavault-db}:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER:-mediavault} -d $${POSTGRES_DB:-mediavault}"]
      interval: 10s
      timeout: 5s
      retries: 10
    security_opt:
      - no-new-privileges:true
    networks:
      - media-db

  mediavault-api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://mediavault:${MEDIAVAULT_DB_PASSWORD:?MEDIAVAULT_DB_PASSWORD is required}@mediavault-postgres:5432/mediavault
      TMDB_API_KEY: ${TMDB_API_KEY:-}
      STEAM_API_KEY: ${STEAM_API_KEY:-}
      STEAM_USER_ID: ${STEAM_USER_ID:-}
      IGDB_CLIENT_ID: ${IGDB_CLIENT_ID:-}
      IGDB_CLIENT_SECRET: ${IGDB_CLIENT_SECRET:-}
      ANNICT_ACCESS_TOKEN: ${ANNICT_ACCESS_TOKEN:-}
      RAKUTEN_APPLICATION_ID: ${RAKUTEN_APPLICATION_ID:-}
      RAKUTEN_ACCESS_KEY: ${RAKUTEN_ACCESS_KEY:-}
      STORAGE_ROOT: /srv/mediavault
      LIBRARY_ROOT: /library
      CORS_ALLOWED_ORIGIN: ${MEDIAVAULT_ALLOWED_ORIGIN:-http://127.0.0.1:8080}
    volumes:
      - ${MEDIAVAULT_STORAGE_SOURCE:-mediavault-storage}:/srv/mediavault
      - ${LIBRARY_SOURCE:-shares}:/library
    depends_on:
      mediavault-postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "--fail", "--silent", "--show-error", "http://localhost:8080/api/v1/health"]
      interval: 15s
      timeout: 5s
      retries: 10
    security_opt:
      - no-new-privileges:true
    networks:
      mediavault-api:
        aliases:
          - backend
      media-db:

  mediavault-mcp:
    build:
      context: ./backend
      dockerfile: mediavault-mcp/Dockerfile
    restart: unless-stopped
    environment:
      MCP_BIND_ADDR: 0.0.0.0:8081
      MCP_AUTH_TOKEN: ${MCP_AUTH_TOKEN:?MCP_AUTH_TOKEN is required}
      MEDIAVAULT_API_BASE_URL: http://mediavault-api:8080
      INTERNAL_API_KEY: ${INTERNAL_API_KEY:-}
      RUST_LOG: ${RUST_LOG:-info}
    ports:
      - "${MCP_BIND_ADDRESS:-0.0.0.0}:${MEDIAVAULT_MCP_PORT:-8081}:8081"
    depends_on:
      - mediavault-api
    healthcheck:
      test: ["CMD", "curl", "--fail", "--silent", "--show-error", "http://localhost:8081/healthz"]
      interval: 30s
      timeout: 3s
      retries: 3
    security_opt:
      - no-new-privileges:true
    networks:
      - mediavault-api

  mediavault-extractor:
    build:
      context: ./extractor
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      MEDIAVAULT_API_BASE_URL: http://mediavault-api:8080
      INTERNAL_API_KEY: ${INTERNAL_API_KEY:?INTERNAL_API_KEY is required}
      EXTRACTOR_LIBRARY_ROOT: /library
      EXTRACTOR_STORAGE_ROOT: /srv/mediavault
      EXTRACTOR_OCR_DEVICE: ${EXTRACTOR_OCR_DEVICE:-cpu}
      EXTRACTOR_MAX_CONCURRENCY: ${EXTRACTOR_MAX_CONCURRENCY:-1}
    volumes:
      - ${MEDIAVAULT_STORAGE_SOURCE:-mediavault-storage}:/srv/mediavault:ro
      - ${LIBRARY_SOURCE:-shares}:/library:ro
    depends_on:
      mediavault-api:
        condition: service_healthy
    security_opt:
      - no-new-privileges:true
    networks:
      - mediavault-api

  mediavault-web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      TZ: ${TZ:-UTC}
    ports:
      - "${BIND_ADDRESS:-127.0.0.1}:${MEDIAVAULT_WEB_PORT:-8080}:80"
    depends_on:
      mediavault-api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 5s
      retries: 5
    security_opt:
      - no-new-privileges:true
    networks:
      - mediavault-api

networks:
  mediavault-api:
  media-db:
    internal: true

volumes:
  mediavault-db:
  mediavault-storage:
  shares:
